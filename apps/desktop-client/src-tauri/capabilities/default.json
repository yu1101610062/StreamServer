{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Allow the configured management center to call the StreamServer desktop bridge.",
  "windows": ["main"],
  "permissions": ["core:default"],
  "remote": {
    "urls": ["http://*:*", "https://*:*"]
  }
}
